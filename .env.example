# Copy to .env (gitignored) for local deploys, or export in your shell:
#   export CLOUDFLARE_ACCOUNT_ID=your_account_id
#
# Get an API token: Cloudflare Dashboard → My Profile → API Tokens
# Template: "Edit Cloudflare Workers" works for Pages deploy, or create custom with:
#   Account → Cloudflare Pages → Edit

CLOUDFLARE_ACCOUNT_ID=
